C# не поддерживает ковариантные вызовы методов с точки зрения модели ООП. В нём ковариантность ограничена только выходным параметром. Реализация метода выбирается 
только по фактическому типу объекта x (полиморфно).
